# What kdos-backup's golden is drawn against. The repository path is never
# opened — the snapshot list comes from testing/fixtures/backup — so this names
# the two things the frame prints and nothing else.
repo    = /var/backup/kdos
include = /home/kdos
