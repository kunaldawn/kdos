# Which portal backend serves which interface, for XDG_CURRENT_DESKTOP=KDOS.
#
# The name of this file is not free: xdg-desktop-portal looks for
# <desktop>-portals.conf with the desktop name lowercased, so KDOS -> kdos.
# Without it the only thing that selects a backend is the UseIn= line inside
# wlr.portal, which lists sway, river, Hyprland and friends and has never heard
# of us — so ScreenCast would come back with no backend at all.
#
# default=none is honest rather than lazy. xdg-desktop-portal-wlr implements
# Screenshot and ScreenCast and nothing else, and the usual second backend is
# xdg-desktop-portal-gtk, which cannot exist here: there is no GTK on the KDOS
# host. So FileChooser, Print and the rest have no backend, boxed apps use
# their own dialogs, and a portal that would only ever fail is not advertised.
# A cell-grid FileChooser in libktui is the eventual answer.
[preferred]
default=none
org.freedesktop.impl.portal.ScreenCast=wlr
org.freedesktop.impl.portal.Screenshot=wlr
