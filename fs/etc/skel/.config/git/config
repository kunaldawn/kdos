# `kdos theme` writes the delta colours beside this file and this
# includes them, so an accent switch never rewrites what you put here.
[include]
	path = kdos-delta
[core]
	pager = delta
[interactive]
	diffFilter = delta --color-only
[delta]
	features = kdos
	navigate = true
[merge]
	conflictStyle = zdiff3
