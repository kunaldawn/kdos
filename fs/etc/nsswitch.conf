# musl ignores this file; it exists for glibc tooling inside distrobox
# containers and for host-integration helpers that consult it.
passwd:     files
group:      files
shadow:     files
hosts:      files dns
networks:   files
protocols:  files
services:   files
ethers:     files
rpc:        files
netgroup:   files
