# Kernel modules for the container layer (podman + distrobox).
# tun: required by slirp4netns for rootless container networking.
# overlay: native overlayfs storage driver (rootful path; rootless uses fuse-overlayfs).
tun
overlay
