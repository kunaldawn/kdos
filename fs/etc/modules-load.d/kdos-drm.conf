# DRM drivers KDOS pre-loads at boot.
# udev modalias autoload is unreliable for some virtio/PCI devices,
# so we load these explicitly. Failures are non-fatal (no matching HW).
virtio_gpu
qxl
bochs
vmwgfx
