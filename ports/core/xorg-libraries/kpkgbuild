# ██╗  ██╗██████╗  ██████╗ ███████╗
# ██║ ██╔╝██╔══██╗██╔═══██╗██╔════╝
# █████╔╝ ██║  ██║██║   ██║███████╗
# ██╔═██╗ ██║  ██║██║   ██║╚════██║
# ██║  ██╗██████╔╝╚██████╔╝███████║
# ╚═╝  ╚═╝╚═════╝  ╚═════╝ ╚══════╝
# ---------------------------------
#   KD's Homebrew Linux Distro
# ---------------------------------

# description	: Meta port for slim Xorg libraries.
# description	: Drops libxaw (Athena widgets — only used by ancient X apps),
# description	: libfs / libfontenc / libxfont2 (legacy bitmap-font path).
# depends	: xtrans libx11 libxext libice libsm libxscrnsaver libxt libxmu libxpm
# depends	: libxfixes libxcomposite libxrender libxcursor libxdamage
# depends	: libxft libxi libxinerama libxrandr libxres libxtst libxv libxvmc
# depends	: libxxf86vm libpciaccess libxkbfile libxshmfence

name=xorg-libraries
version=1.0
release=2

build() {
	mkdir -pv $PKG/etc
}
