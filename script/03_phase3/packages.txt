# ██╗  ██╗██████╗  ██████╗ ███████╗
# ██║ ██╔╝██╔══██╗██╔═══██╗██╔════╝
# █████╔╝ ██║  ██║██║   ██║███████╗
# ██╔═██╗ ██║  ██║██║   ██║╚════██║
# ██║  ██╗██████╔╝╚██████╔╝███████║
# ╚═╝  ╚═╝╚═════╝  ╚═════╝ ╚══════╝
# ---------------------------------
#   KD's Homebrew Linux Distro
# ---------------------------------
#   Phase 3: Developer Toolchain
# ---------------------------------
#
# This is the heart of KDOS. The system is self-hosting: Phase 3
# provides every compiler, build system, language runtime, and
# spec-tooling needed to rebuild KDOS from KDOS.

# ---------------------------------
# Build Toolchain
# ---------------------------------
musl
gcc
binutils
make
pkgconf
libtool
autoconf
automake
m4
bison
flex

# ---------------------------------
# Modern Build Systems
# ---------------------------------
cmake
meson
ninja

# ---------------------------------
# Languages & Runtimes
# ---------------------------------
python3
perl
# A Makefile rule that pipes `$(PROG) --help` into help2man does not degrade
# when it is absent — it exits 127 and takes the build with it.
help2man
lua
# 5.4 BESIDE 5.5, not instead of it. Prosody 13 supports 5.2 through 5.4 and
# will not load on 5.5; every path this installs is versioned, so neither
# package can shadow the other.
lua54
# The four modules Prosody will not start without: a transport, TLS over it,
# an XML parser (XMPP is XML on the wire) and stat/mkdir/readdir.
lua54-luasocket
lua54-luasec
lua54-luaexpat
lua54-luafilesystem
nodejs
rust
cargo-c
cbindgen
bindgen
yasm
nasm
swig
cython3
# Additional languages (kpkgbuild scaffolds — verify build in chroot):
go
zig

# ---------------------------------
# Alternative compilers / linkers
# ---------------------------------
# clang and llvm are kept as developer tools (not strictly needed
# for self-hosting). Useful for projects that prefer LLVM, and
# `lldb` debugger ships with llvm.
clang
llvm
lld

# ---------------------------------
# Essential Utilities
# ---------------------------------
bash
toybox
diffutils
patch
findutils
gawk
# GNU sed, over toybox's applet, for the same reason as gawk and findutils
# above: upstream build systems assume GNU extensions. The case that forced it
# was xapian, whose configure generates its public version.h through a sed
# script using `0,/regex/d` — toybox does not implement that address form,
# produced nothing, and left a ZERO-BYTE header that failed three steps later
# with an error naming a type. Nothing in it said "sed".
sed
tar
xz
gzip
bzip2
zstd
lzo
lz4
libarchive

# ---------------------------------
# Network & Security
# ---------------------------------
curl
git
ca-certificates
openssl

# ---------------------------------
# Base Libraries & Database
# ---------------------------------
zlib
expat
libffi
libxml2
libxslt
sqlite
ncurses
readline

# ---------------------------------
# System Infrastructure
# ---------------------------------
util-linux
shadow
linux-pam
kmod
eudev
attr
acl
libcap
bc
elfutils
gperf
intltool
itstool
gettext
patchelf
ccache

# ---------------------------------
# Documentation & Spec Tooling
# ---------------------------------
asciidoc
xmlto
docbook-xml
docbook-xsl
scdoc
