# ██╗  ██╗██████╗  ██████╗ ███████╗
# ██║ ██╔╝██╔══██╗██╔═══██╗██╔════╝
# █████╔╝ ██║  ██║██║   ██║███████╗
# ██╔═██╗ ██║  ██║██║   ██║╚════██║
# ██║  ██╗██████╔╝╚██████╔╝███████║
# ╚═╝  ╚═╝╚═════╝  ╚═════╝ ╚══════╝
# ---------------------------------
#   KD's Homebrew Linux Distro
# ---------------------------------
#   Phase 5: The desktop
# ---------------------------------
#
# Everything the desktop is, and nothing the userland already provides.
# The Wayland base (wayland, wayland-protocols, seatd, foot, grim, slurp,
# wl-clipboard, xwayland) is phase 4 — it is useful without a compositor.
#
# See docs/KDOS-DESKTOP.md.

# The compositor library. Pinned and vendored; wlroots breaks API every
# release. Xwayland's xwm needs ICCCM and EWMH, which is xcb-util-wm.
xcb-util-wm
wlroots

# The compositor. src/desktop is on PORT_REPO for this phase only.
kdos-comp

# The security-context-v1 sandbox engine: one tagged Wayland socket per box.
# Separate from kdos-appbox because `run` execs distrobox and cannot hold the
# context's close fd, and because kdos-appbox links no libraries.
kdos-boxsock

# The shell: a character-cell panel on layer-shell, drawn by libktui through
# libkwl. Learns what it shows from wlr-foreign-toplevel-management and
# ext-workspace-v1 — standard protocols, so waybar works here too.
kdos-shell

# The console session: windows, terminals and native surfaces on a character
# grid, with NO display of its own. It rasterises no glyph and opens no device
# — that is kdos-view's half — which is what lets it come up on a machine whose
# GPU driver does not. Placement, tiling and the edge search are libkwm's, the
# same ones kdos-comp calls.
kdos-con

# The display half, and the reason the session has none. A view holds no
# window list, no focus and no clipboard: cells arrive and input leaves. That
# is what makes detach and reattach, a second seat, a desktop over ssh and the
# rig's exact-cell screenshots all fall out of one split rather than be built
# four times.
kdos-view

# The terminal. ONE BINARY, TWO DESKTOPS: libkdisp picks between libkwl and
# libkcon at startup, so the same source is an xdg-toplevel under kdos-comp and
# a cell surface under kdos-con. libkvt is the state machine — kmscon's, forked
# — and libkimg is what turns sixel, iTerm2's OSC 1337 and kitty's APC into
# pixels.
#
# It ships BESIDE foot and changes nothing: foot is still the default terminal
# in rc.xml, menu.xml, the .desktop files, favorites and tmux.conf. foot is
# battle-tested and this is not, which is the same trade kdos-res made beside
# btop.
kdos-term

# One application, full screen, on a VT of its own — a hard fork of cage 0.3.1,
# which is MIT and is built on the same wlroots 0.20 the compositor fork pins.
# It is how a Wayland application reaches the console desktop: that desktop
# composites character cells and a Wayland client's surface is pixels, so the
# guest gets a terminal rather than a window.
#
# It links wlroots and, of ours, only libkcolor — statically, for the palette
# its background is painted in.
kdos-cage

# The lock screen, and one of the two setuid-root binaries KDOS ships:
# /etc/shadow is root-only and kdos-lock must not be. ext-session-lock-v1 means the compositor
# keeps the screen locked even if the client dies.
kdos-lock

# ---------------------------------
# The resource monitor
# ---------------------------------
# One binary that is a TTY program on tty1 and an xdg-toplevel window on the
# desktop, because both are the same cell grid: nine per-device pages, a
# sortable process table, and an application rollup that names a boxed app by
# the container supervising it — an identity no other Linux monitor can build
# without systemd cgroup delegation, which this distro does not have.
#
# It ships BESIDE btop rather than replacing it: btop is mature, and until this
# has run on real hardware a fallback is worth keeping.
#
# kdos-resctl is built by the same recipe and is the SECOND setuid bit this
# distro ships — three verbs, no paths in argv, gated on `wheel` by real uid.
kdos-res

# Suspend, poweroff and reboot for a desktop that is not root. A unix socket in
# /run, gated by SO_PEERCRED — no setuid, no polkit prompt, no logind.
kdos-powerd

# Per-app Energy Impact. RAPL is root-only (PLATYPUS), so the sampler is a
# daemon; identity comes free from the fact that every alien app is already its
# own container. Relative shares, never watt-hours.
kdos-energyd

# Wedge protection under memory pressure. Blocks on a /proc/pressure/memory PSI
# trigger and SIGKILLs + process_mrelease()s the largest attributable offender —
# preferring boxed apps, never the desktop's own chrome — at most once per ten
# seconds. Same shape as kdos-powerd: root, foreground under ksvc, SO_PEERCRED
# socket for `kdos doctor`.
kdos-oomd

# Removable media. Plugging a stick into this machine did nothing at all —
# there is no udisks here, mounting is root's and the desktop is not root.
# Same shape as kdos-powerd: root, foreground under ksvc, an SO_PEERCRED
# socket, and a protocol whose only argument is a row number out of a list the
# daemon published, so a client can never name a device or a mountpoint.
kdos-mountd

# Packs. An application is one signed EROFS image with a KDOS footer on the
# end, and this is the only thing on the machine that mounts one: it verifies
# where it mounts, composes a box's root filesystem out of an overlay of them,
# and takes an id out of the list it published rather than a path. Mounting as
# ROOT is what makes overlay whiteouts real, which is what removes the flatten
# step the monolithic image needed.
erofs-utils
kdos-pack
kdos-packd

# The input method. kdos-comp speaks text-input-v3 to applications and
# input-method-v2 to fcitx5 (the fork's src/input/ime.c); this is the
# thing on the other end of that second protocol. Built Wayland-only — no X11
# frontend, no Qt config tool, no cloud pinyin — and the engines are one per
# language: chinese-addons (pinyin, shuangpin, tables) over libime, anthy for
# Japanese, hangul for Korean.
fcitx5
fcitx5-chinese-addons
fcitx5-anthy
fcitx5-hangul

# The screen-capture portal. A boxed OBS or browser reaches the screen only
# through xdg-desktop-portal, which has no capture code of its own — the wlr
# backend is what turns the compositor's ext-image-copy-capture and
# wlr-screencopy globals into a ScreenCast session. Screenshot comes with it.
# Phase 4 installs the main portal; this is the half that knows about wlroots.
xdg-desktop-portal-wlr

# The FileChooser portal, which nothing served before this: xdg-desktop-portal-wlr
# does ScreenCast and Screenshot only, and the backend every other desktop falls
# back to is xdg-desktop-portal-gtk — impossible here, because there is no GTK on
# the host. Until this existed, every boxed app's Open and Save dialog was its
# own toolkit's, which put a rounded antialiased GTK dialog in the middle of a
# text-mode desktop. Settings comes with it and hands out the accent.
xdg-desktop-portal-kdos
